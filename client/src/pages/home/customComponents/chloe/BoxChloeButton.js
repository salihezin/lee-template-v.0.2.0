const BoxChloeButton = ({ href, buttonLabel }) => {
  return (
    <>
      <a className="button button-sm button-default button-ujarak" href={href} >{buttonLabel}</a>
    </>
  );
}

export default BoxChloeButton;
